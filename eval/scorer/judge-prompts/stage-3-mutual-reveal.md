# Judge Prompt: stage-3-mutual-reveal

Score whether the AI presents both needs summaries side by side and asks what the user notices without labeling overlap. Return JSON with dimensions `side_by_side_reveal`, `user_noticing`, and `no_ai_analysis`.
