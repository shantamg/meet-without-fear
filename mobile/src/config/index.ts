/**
 * Config barrel export
 *
 * Re-exports all configuration objects and types for clean imports.
 */

// Waiting status UI configuration
export {
  getWaitingStatusConfig,
  getWaitingBannerText,
  WAITING_STATUS_CONFIG,
  type WaitingStatusConfig,
} from './waitingStatusConfig';
